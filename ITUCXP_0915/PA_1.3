void pelda_2(char *fnev1, char *fnev2)
{
         FILE *fp1, *fp2; 
         char ch; 
         int pos; 
         if ((fp1 = fopen(fnev1, "r")) == NULL)
         {
            printf("\n Nem lehet megnyitni a fajlt.");
            return;
         }
         else
         { 
            printf("\n Fajl megnyitva masolasra…\n "); 
         } 
         fp2 = fopen(fnev2, "w"); 
         fseek(fp1, 0L, SEEK_END);   // File pointer a fajl vegere pos = ftell(fp1); 
         fseek(fp1, 0L, SEEK_SET);   // File pointer a fajl elejere while (pos--)
         {
            ch = fgetc(fp1);
            fputc(ch, fp2);
         }
         fclose(fp1);
         fclose(fp2);
}    
